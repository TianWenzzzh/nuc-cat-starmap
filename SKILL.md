---
name: "中北大学—校园猫咪档案Skill"
description: "把校园流浪猫实地普查数据做成可交互的猫咪星系网页星图：每只猫是一颗星（星色取毛色、星等取实拍数、星位为真实出没区），含军衔编制档案卡、喵星导览自动剧场、实地寻猫分区目录与寻猫海报导出、今日喵签、喵图鉴收集、星轨长曝光、极光首页与分享卡。当用户想看中北校园猫档案、漫游喵星图、看导览、按区寻猫、抽喵签、集图鉴，或想为其他学校复刻同类猫咪可视化档案时调用。触发短语：中北喵星图、看看中北的猫、打开喵星图、校园猫档案、中北猫咪档案、喵星导览、实地寻猫、寻猫海报、抽个喵签、喵图鉴。"
---

# 校园猫咪星图 · Campus Cat Star-map

从实地普查、结构化名册、交互星图、位置校准到打包分发，这套做法可以整体搬到任何一所学校。
当前实例：中北大学，在册76只，实拍194张，2026-08普查。

## 一、数据规范（猫咪名册.csv）

每只猫一条记录，字段和星图行为一一对应：

| 字段 | 说明 | 星图映射 |
|------|------|----------|
| id | `CAT-001` 递增，跳号保留存疑位 | 稳定种子（位置随机、闪烁相位） |
| name / rank / title | 昵称 / 军衔 / 岗位 | 档案卡头部、搜索命中 |
| coat / coatGroup | 具体毛色 / 大组（橘、狸花、三花、黑白、其他） | 星色与筛选chips |
| features / area / bio | 辨认特征 / 出没区域 / 50-80字小传 | 档案卡正文 |
| photo / photoCount | 代表照相对路径 / 收录张数 | 卡片大图 / 星等与环绕光点 |
| brightness | 0.5-1.05 代表照明度 | 星点亮度 |

身份存疑的候选一律不收录，宁可少一只，不放一只错的。弃用的编号跳号保留，决策记进归并文档。

## 二、生成与校准流程

1. 建图：底图用夜空暗化版校园地图，坐标为归一化 `x,y ∈ [0,1]`。
2. 落位：`catPos` 优先级是 手动校准(localStorage) > CALIB固化表 > 种子随机。
3. 校准：开"校准位置"，拖拽星星到真实出没区，"导出校准数据"得到JSON，
   把坐标誊进HTML内`CALIB`表并清掉localStorage，位置就固化了（解压即用，不依赖本地存储）。
4. 落位约束：新星与既有星距离不低于0.05（归一化坐标）；同一区域的猫相邻落位；
   避开左侧山区、底部河流、右侧图例区。

## 三、视觉规范（诗云式）

- 三相主循环：`intro`（粒子汇聚成标题 + 三色极光幕帘）、`fly`（深空拉镜2.6秒 + 星星按离焦距离依次点亮）、`live`。
- 首页：三色极光垂帘（青、紫、金，底边最亮、竖纹闪烁、径向暗角），粒子标题用同款金青紫色谱。
- 星系：三旋臂差速旋转螺旋星系、预渲染星云、毛色聚簇星云（簇色取主流毛色）、实况天色（按时辰）、双层视差星尘、全程流星雨。
- 星轨长曝光（可选开关）：星系绘制在独立离屏层，轨迹缓冲只累积星系层（`lighter`叠加 + 黑色透明衰减`.018`），
  星系转速提至3.8倍、沉积加粗2.6倍拉出弧形光带；地景透明度降到38%作鬼影。千万别累积整帧，会把地图和UI糊成白雾。
- 星点：外晕、星芒、内辉三层发光，环绕光点数等于照片数，带呼吸微动。
- 性能：发光用sprite缓存；`dt`步进防高刷屏加速；DPR不超过2。

## 四、互动与留存

- 喵星导览：一键剧场模式，镜头自动飞抵六位旗舰猫（墩墩、玄铁、摔跤、陪练、初雪、独眼），字幕打字机逐站讲解，
  轻触字幕跳下一站，任意开卡即自动退场。零额外资源，就是flyTo加setTimeout序列。
- 实地寻猫：12大出没区分组目录（`area`字段关键词语义归组优先，未命中回退坐标就近锚点），
  每区列出常驻猫缩略图、军衔、识别特征，点击直达它的星。"导出寻猫海报"是纯矢量Canvas竖版PNG
  （不画照片和底图，规避file://画布污染），存手机就能带去实地。
- 喵图鉴：开卡打卡（localStorage持久化），已收集的星戴金环、带进度条；满10/30/50/76只触发里程碑流星雨。
- 今日喵签：按`Math.floor(Date.now()/864e5)`确定性抽取猫与签文，同一天全站结果一致。
- 鼠标彗星（`pointer:fine`才启用）：缓动跟随，尾长随移速伸展，纯Canvas绘制。
- 访客彗星：周期到访某只猫的星域，贝塞尔路径掠过并短暂点亮该星域，toast报信。
- 星语环境音（默认关）：WebAudio全生成，低频drone、LFO滤波、延迟回声、事件拨音，零音频文件。

## 五、唤起方式（激活短语）

**直接体验**：浏览器打开在线版 <https://gdfmdvcmsj.coze.site>，或解压后双击`中北喵星图.html`，两者功能完全一致，数据全部内嵌。

在WorkBuddy对话框输入以下任一短语即可唤起本Skill：

- 中北喵星图 ／ 打开喵星图 ／ 中北猫咪档案
- 看看中北的猫 ／ 校园猫档案 ／ 猫咪图鉴
- 抽个喵签 ／ 喵图鉴 ／ campus cat star-map
- 喵星导览 ／ 实地寻猫 ／ 寻猫海报

## 六、打包分发

```
中北大学—校园猫咪档案Skill/
├── SKILL.md                本文件，方法论
├── README.md               使用说明
├── 归并决策摘要.md          归并取舍记录
├── 中北喵星图.html          交付物，纯前端离线可用
├── 校园地图-夜空版-web.jpg   底图
├── data/猫咪名册.csv        数据源
└── assets/                 压缩照片 + 图标icon.jpg
```

打包时把HTML内`../照片视频/`这类外链重写为`assets/photos/`，只压缩实际被引用的照片（最长边1100、q80），控制zip体积。
