---
name: "中北大学—校园猫咪档案Skill"
description: "把校园流浪猫实地普查数据做成可交互的『猫咪星系』网页星图：每只猫=一颗星（星色取毛色·星等取实拍数·星位=真实出没区），含军衔编制档案卡、喵星导览自动剧场、实地寻猫分区目录与寻猫海报导出、今日喵签、喵图鉴收集、星轨长曝光、极光首页与一键分享卡。当用户想看中北校园猫档案、漫游喵星图、看导览、按区寻猫、抽喵签、集图鉴，或想为其他学校/社区复刻猫咪可视化档案时调用。输入「中北喵星图」「看看中北的猫」「打开喵星图」「校园猫档案」「中北猫咪档案」「喵星导览」「实地寻猫」「寻猫海报」「抽个喵签」「猫咪图鉴」等短语即可唤起。Campus cat census → interactive star-map archive. Triggers: 中北喵星图, 看看
中北的猫, 打开喵星图, 校园猫档案, 中北猫咪档案, 喵星导览, 实地寻猫, 寻猫海报, 抽喵签, 喵图鉴, campus cat star-map."
---

# 校园猫咪星图 · Campus Cat Star-map

将「实地普查 → 结构化名册 → 交互星图 → 位置校准 → 打包分发」固化为一条可复用流水线。
当前实例：中北大学 76 只在编基米 / 194 张学长学姐实拍 / 2026-08 实地普查。

## 一、数据规范（猫咪名册.csv）

每只猫一条记录，字段与星图行为一一对应：

| 字段 | 说明 | 星图映射 |
|------|------|----------|
| id | `CAT-001` 递增，跳号保留存疑位 | 稳定种子（位置随机、闪烁相位） |
| name / rank / title | 昵称 / 军衔 / 岗位 | 档案卡头部、搜索命中 |
| coat / coatGroup | 具体毛色 / 大组（橘·狸花·三花·黑白·其他） | **星色**与筛选 chips |
| features / area / bio | 辨认特征 / 出没区域 / 50-80字小传 | 档案卡正文 |
| photo / photoCount | 代表照相对路径 / 收录张数 | 卡片大图 / **星等与环绕光点** |
| brightness | 0.5-1.05 代表照明度 | 星点亮度 |

质量红线：低置信候选（疑似重复/身份存疑）宁缺毋滥，跳号弃用并记录在归并决策文档。

## 二、生成与校准流程

1. **建图**：底图用夜空暗化版校园地图；坐标为归一化 `x,y ∈ [0,1]`。
2. **落位**：`catPos` 优先级 = 手动校准(localStorage) > CALIB 固化表 > 种子随机。
3. **校准**：开启「✎ 校准位置」→ 拖拽星星到真实出没区 → 「⬇ 导出校准数据」得到 JSON →
   将坐标誊入 HTML 内 `CALIB` 表并清掉 localStorage，实现**位置固化**（解压即用，不依赖本地存储）。
4. **落位硬约束**：
   - 新星与既有星距离 ≥ 0.05（归一化坐标）；
   - 同一聚簇（出没区域相同）的猫相邻落位；
   - 避开左侧山区、底部河流、右侧图例区。

## 三、视觉规范（诗云式）

- 三相主循环：`intro`（粒子汇聚成标题 + 三色极光幕帘）→ `fly`（深空拉镜 2.6s + 星星按离焦距离依次点亮）→ `live`。
- 首页：三色极光垂帘（青·紫·金，底边最亮、竖纹闪烁、径向暗角）+ 粒子标题「金→青→紫」色谱呼应。
- 星系：三旋臂差速旋转螺旋星系 + 预渲染星云 + 毛色聚簇星云（簇色=主流毛色）+ 实况天色（按时辰）+ 双层视差星尘 + 全程流星雨。
- 星轨长曝光（可选开关）：星系绘制于独立离屏层，轨迹缓冲**只累积星系层**（`lighter` 叠加 + 黑色透明衰减 `.018`），
  星系转速提至 3.8×、沉积加粗 2.6× 拉出弧形光带；地景透明度降至 38% 作鬼影。切勿累积整帧（会把地图/UI 糊成白雾）。
- 星点：外晕/星芒/内辉三层发光，环绕光点=照片数，呼吸微动。
- 性能：发光用 sprite 缓存；`dt` 步进防高刷屏加速；DPR≤2。

## 四、互动与留存

- **喵星导览**：一键剧场模式——镜头自动飞抵 6 位旗舰猫（墩墩/玄铁/摔跤/陪练/初雪/独眼），字幕打字机逐站讲解，
  轻触字幕跳下一站；任意开卡即自动退场。零额外资源，纯 flyTo + setTimeout 序列。
- **实地寻猫**：12 大出没区分组目录（`area` 字段关键词语义归组优先，未命中回退坐标就近锚点），
  每区列出常驻猫缩略图 + 军衔 + 识别特征，点击直达它的星；「一键导出寻猫海报」为纯矢量 Canvas 竖版 PNG
  （不画照片/底图，规避 file:// 画布污染），可存手机带去实地。
- **喵图鉴**：开卡打卡（localStorage 持久化），已收集星戴金环 + 进度条；里程碑 10/30/50/76 触发流星雨。
- **今日喵签**：按 `Math.floor(Date.now()/864e5)` 日期确定性抽取猫与签文模板，同一天全站结果一致，无随机抖动。
- **鼠标彗星**（`pointer:fine` 才启用）：缓动跟随 + 尾长随移速伸展，纯 Canvas 绘制。
- **访客彗星**：周期到访某只猫的星域，贝塞尔路径掠过并短暂点亮该星域，toast 报信。
- **星语环境音**（默认关）：WebAudio 全生成（低频 drone + LFO 滤波 + 延迟回声 + 事件拨音），零音频文件。

## 五、唤起方式（激活短语）

**直接体验**：浏览器打开在线版 <https://gdfmdvcmsj.coze.site>，或解压后双击 `中北喵星图.html`——两者功能完全一致，数据全部内嵌。

在 WorkBuddy 对话框输入以下任一短语即可唤起本 Skill：

- 中北喵星图 ／ 打开喵星图 ／ 中北猫咪档案
- 看看中北的猫 ／ 校园猫档案 ／ 猫咪图鉴
- 抽个喵签 ／ 喵图鉴 ／ campus cat star-map
- 喵星导览 ／ 实地寻猫 ／ 寻猫海报

## 六、打包分发

```
中北大学—校园猫咪档案Skill/
├── SKILL.md                ← 本文件（方法论）
├── README.md               ← 使用说明
├── 归并决策摘要.md          ← 数据治理留痕
├── 中北喵星图.html          ← 交付物（纯前端 · 离线可用）
├── 校园地图-夜空版-web.jpg   ← 底图
├── data/猫咪名册.csv        ← 数据源
└── assets/                 ← 压缩照片 + 装饰图标 icon.jpg（AI 生成的装饰物料）
```

要点：打包时把 HTML 内 `../照片视频/` 等外链重写为 `assets/photos/`，仅压缩**实际被引用**的照片（最长边 1100 / q80），控制 zip 体积。
